let button = document.querySelector('.madeField');button.addEventListener('click', function () {    (function () {        var table = document.createElement('table');        var val = document.getElementById('inp1').value        var min = document.getElementById('inp2').value        var arr = [];        var array = [];        if (val < 4) { val = 4; }        if (val > 8) { val = 8; }        for (var i = 0; i < val; i++) {            array[i] = [];            for (var j = 0; j < val; j++){                array[i][j] == 0;            }        }        for (var i = 0; i < min; i++) {            var rand = getRandomIntInclusive(0, val);            var rand1 = getRandomIntInclusive(0, val);            array[rand][rand1] = 1;            // поправить рандомайзер чтобы не было повторений        }        for (var i = 0; i < val; i++) {            var tr = document.createElement('tr');            arr[i] = [];            for (var j = 0; j < val; j++) {                if (array[i][j] != 1) {                    var td = document.createElement('td');                    var div = document.createElement("div");                    div.style.width = "61px";                    div.style.height = "61px";                    div.style.border = "0.1px solid rgba(0, 0, 0, 1)";                    div.style.boxShadow = "inset 7px 7px 4px rgba(255, 255, 255, 0.25), inset -6px -4px 8px rgba(0, 0, 0, 0.25)";                    div.style.display = "inline-block";                    div.style.margin = "0";                    div.style.paddingBottom = "0em";                    td = arr[i][j] = div;                }                else {                    var td = document.createElement('td');                    var div = document.createElement("div");                    div.style.width = "61px";                    div.style.height = "61px";                    div.style.border = "1px solid rgba(255, 0, 0, 1)";                    div.style.boxShadow = "inset 7px 7px 4px rgba(255, 0, 0, 1), inset -6px -4px 8px rgba(255, 0, 0, 1)";                    div.style.display = "inline-block";                    div.style.margin = "0";                    div.style.paddingBottom = "0em";                    div.style.background = "ff0000";                    td = arr[i][j] = div;                }                tr.appendChild(td);            }            table.appendChild(tr);            table.style.borderCollapse = "collapse";        }        document.getElementById('matrix').appendChild(table);    })();});function getRandomIntInclusive(min, max) {    min = Math.ceil(min);    max = Math.floor(max);    return Math.floor(Math.random() * (max - min + 1)) + min; //Максимум и минимум включаются}let button1 = document.querySelector('.enter');button1.addEventListener('click', function () {    (function () {        var table = document.createElement('table');        var val = document.getElementById('inp1').value        var min = document.getElementById('inp2').value        var arr = [];        var array = [];        if (val < 4) { val = 4; }        if (val > 8) { val = 8; }        for (var i = 0; i < val; i++) {            array[i] = [];            for (var j = 0; j < val; j++) {                array[i][j] == 0;            }        }        for (var i = 0; i < min; i++) {            var rand = getRandomIntInclusive(0, val);            array[rand][rand] = 1;            // поправить рандомайзер чтобы не было повторений и выводил два числа        }        for (var i = 0; i < val; i++) {            var tr = document.createElement('tr');            arr[i] = [];            for (var j = 0; j < val; j++) {                if (array[i][j] != 1) {                    var td = document.createElement('td');                    var div = document.createElement("div");                    div.style.width = "61px";                    div.style.height = "61px";                    div.style.border = "0.1px solid rgba(0, 0, 0, 1)";                    div.style.boxShadow = "inset 7px 7px 4px rgba(255, 255, 255, 0.25), inset -6px -4px 8px rgba(0, 0, 0, 0.25)";                    div.style.display = "inline-block";                    div.style.margin = "0";                    div.style.paddingBottom = "0em";                    td = arr[i][j] = div;                }                else {                    var td = document.createElement('td');                    var div = document.createElement("div");                    div.style.width = "61px";                    div.style.height = "61px";                    div.style.border = "1px solid rgba(0, 0, 0, 0.83)";                    div.style.boxShadow = "inset 7px 7px 4px rgba(255, 255, 255, 0.25), inset -6px -4px 8px rgba(0, 0, 0, 0.25)";                    div.style.display = "inline-block";                    div.style.margin = "0";                    div.style.paddingBottom = "0em";                    div.style.background = "ff0000";                    td = arr[i][j] = div;                }                tr.appendChild(td);            }            table.appendChild(tr);            table.style.borderCollapse = "collapse";        }        document.getElementById('matrix1').appendChild(table);    })();});